#include "main.h"
/**
 * _abs - the absolute value of an integer
 * @c : integer or number 
 * Return: integer abs of the value
 **/
int _abs(int c)
{
	if (c < 0)
		return (-c);
	else
		return (c);
}
